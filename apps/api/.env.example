# Server
PORT=8080
NODE_ENV=development
TZ=Asia/Tashkent
CORS_ORIGIN=http://localhost:5173

# DB
DATABASE_URL=postgres://postgres:postgres@localhost:5432/work_control

# Auth
JWT_SECRET=change_me_super_secret
JWT_EXPIRES_IN=7d

# Auto-init schema (dev uchun qulay)
DB_AUTO_INIT=true

# Create default admin on boot (if not exists)
ADMIN_NAME=Admin
ADMIN_PHONE=998934040902
# Admin paroli (ixtiyoriy). Agar bo‘sh bo‘lsa: phone last4 ishlatiladi.
ADMIN_PASSWORD=0902
